{
  "name": "collide-testgen",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "SP3 test-data pipeline: generates reproducible hidden test bundles for LeetCode 150 problems.",
  "scripts": {
    "build-bundles": "tsx src/cli/build-bundles.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
